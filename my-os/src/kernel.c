/**
 * @file kernel.c
 * @brief This is the main function for my kernel.
 *        It's the first C function that gets executed.
 */
void kernel_main(void) {
    /*
     * I'll put my kernel code here.
     * For now, I'm just using an infinite loop to prevent the system from halting.
     */
    while (1) { }
}